# Handoff — 2603171155

## This Session

### Commit 1 (2603171155) — systemd 서비스 + 종료 정리 + 팝아웃 터미널

**systemd user 서비스 등록**
- `~/.config/systemd/user/mpm-dashboard.service` 생성
- 시스템 파이썬(`/usr/bin/python3`)으로 대시보드 상시 구동
- `Restart=on-failure` (5초 후 자동 재시작), `loginctl enable-linger`로 로그인 없이도 실행
- 관리: `systemctl --user {start|stop|restart|status} mpm-dashboard`

**서버 종료 시 전체 세션 정리**
- `gateway/session_manager.py`에 `cleanup_all()` 추가 — `mpm-` 접두사 tmux 세션 + ttyd 프로세스 전부 kill
- `server.py`에 SIGTERM/SIGINT 시그널 핸들러 + atexit 등록
- 배경: 서버 꺼져도 tmux 세션이 살아남으면 사용자가 잔존 세션을 인지할 방법이 없음

**팝아웃 터미널** (이전 작업 포함)
- 터미널 헤더에 팝아웃 버튼(↗) 추가 — 독립 브라우저 윈도우로 분리
- `terminal.html` 전면 재작성: iframe → xterm.js + ttyd WebSocket 직접 연결
- 팝아웃 ↔ 패널 도킹 전환, 지오메트리(위치/크기) localStorage 저장/복원
- 팝아웃 창에서도 Shift+Space 세션 순환, Start/Kill CLI 동작
- `window.postMessage`로 메인 페이지와 팝아웃 간 통신 (dock, cycle, close 이벤트)

### Commit 2 (2603171200) — Since Last Handoff 기준 수정

- `projects.py`의 unhandoffed commits 기준을 파일 mtime → git 커밋 타임스탬프로 변경
- 기존: 핸드오프 파일의 디스크 mtime 기준 → 핸드오프 커밋 자체가 "Since Last Handoff"에 남음
- 수정: `git log -1 --format=%ct -- <handoff file>`로 커밋 시점을 기준으로 사용 → 핸드오프 직후 목록이 비어있음

## Previous Sessions

2603161538 — 키보드 UX 설계(레이어/존 모델), Shift+Space 세션 순환, xterm.js 직접 연결 + 터미널 캐싱, tmux mouse 복원, 터미널 UX 개선 (딤/Esc닫기/좀비ttyd정리)

2603161218 — 터미널 UX 버그 수정 (스크롤/우측패널/드래그/위치저장/오버레이), xterm.js PTY bridge + CLI 시작 다이얼로그 + 터미널 카드, ttyd 전환 + inotify 파일 감지 + 터미널 패널 리사이즈/우측 토글

2603151818 — 포스트잇 노트, 문서 한글화 키워드 복원, Phase 2 ROADMAP 재정의, tmux 세션 관리 + Gateway API, WebSocket multiplexer + 터미널 뷰

2603141524 — Phase 1 대시보드 구축 + UI 개선

2603140119 — 프로젝트 스캐폴드, ROADMAP 수정

## Next Tasks

- [ ] 키보드 조작 UX 완성 — 존 네비게이션 (좌우 컬럼 이동, 상하 아이템 이동, Enter/Space 활성화)
- [ ] Agent 상태 세분화 (응답 중 / 응답 완료 / 유휴 / 꺼짐)
- [ ] 응답 완료 인터랙션
- [ ] 비서 Agent
- [ ] Telegram 브릿지
