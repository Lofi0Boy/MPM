# Handoff — 2603161121

## This Session

### Commit 1 (2603151818) — xterm.js PTY bridge + CLI 시작 다이얼로그 + 터미널 카드

- xterm.js + `gateway/pty_bridge.py`로 터미널 구현 (이후 ttyd로 교체됨)
- CLI 시작 다이얼로그: 세션 없을 때 터미널 버튼 클릭 시 팝오버로 저장된 커맨드 선택 + 커스텀 입력 + `+` 저장
- 터미널 카드: 컬럼 헤더의 아이콘 대신 스트림 맨 위에 전용 카드 배치, border 색상으로 상태 표시
- Kill CLI 버튼 + 카드 ■ 중지 버튼
- 외부 CLI 감지 제거 — MPM tmux 세션(mpm- prefix)만 인식
- `data/cli_patterns.json`에 `ssh_host`, `saved_commands` 필드 추가

### Commit 2 (2603161121) — ttyd 전환 + inotify 파일 감지 + 터미널 패널 리사이즈/우측 토글

직접 구현한 PTY bridge(pty_bridge.py)를 버리고 ttyd로 전환:

- **ttyd 통합**: 세션 생성 시 프로젝트별 ttyd 프로세스 자동 시작 (포트 자동 할당), 세션 종료 시 ttyd도 종료. 대시보드에서 iframe으로 ttyd 임베드
- **inotify 파일 감지**: `gateway/file_watcher.py` — watchdog 라이브러리로 각 프로젝트 docs 디렉토리 감시, 파일 변경 시 SocketIO로 `projects_changed` emit. 30초 폴링 제거 (fallback 120초 유지)
- **터미널 패널 리사이즈**: 상단 가장자리 드래그로 높이 조절
- **하단/우측 토글**: ↔ 버튼으로 터미널 패널 위치 전환, 우측에서도 좌측 가장자리 드래그로 너비 조절
- **render 후 즉시 updateStatusDots()** 호출로 상태 깜빡임 수정

## Previous Sessions

2603151818 — 포스트잇 노트, 문서 한글화 키워드 복원, Phase 2 ROADMAP 재정의, tmux 세션 관리 + Gateway API, WebSocket multiplexer + 터미널 뷰

2603141524 — Phase 1 대시보드 구축 + UI 개선

2603140119 — 프로젝트 스캐폴드, ROADMAP 수정

## Next Tasks

- [ ] 웹 터미널 스크롤 문제 해결 (ttyd 기반으로 재논의)
- [ ] 키보드 조작 UX
- [ ] 프로젝트별 세션 상태 시각화 UI 개선
- [ ] Agent 상태 세분화 (응답 중 vs 응답 완료 vs 유휴)
- [ ] 비서 Agent — MpmWorkspace cwd 채팅
- [ ] Telegram 브릿지
