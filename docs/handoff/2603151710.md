# Handoff — 2603151710

## This Session

### Commit 1 (2603151211) — 포스트잇 노트 기능

대시보드에 아이디어 캡처용 스티키 노트 시스템 추가.

- `data/ideas.json` — 노트 영구 저장소 (위치, 색상, 프로젝트 소속)
- CRUD API: `GET/POST /api/ideas`, `PUT/DELETE /api/ideas/<id>`, promote API
- 드래그 가능 포스트잇, 4색 테마, 더블클릭 펼침/접힘, 프로젝트 소속 자동 감지
- `+ Note` 버튼, `ESC`로 접기

### Commit 2 (2603151211) — 문서 한글화 키워드 복원

사용자가 문서를 한글화했으나, 파서가 영어 구조적 헤더(`## Phase N:`, `Goal:`, `## This Session`, `### Commit N` 등)만 인식하는 문제 발생.

- 4개 프로젝트(MPM, JHomelab_app, JHomelab_server, saksak-kimchi)의 ROADMAP + 핸드오프 파일에서 구조적 헤더를 영어로 복원
- MpmWorkspace CLAUDE.md에 "Document Language Rules" 섹션 추가 — 파서 인식용 키워드는 영어 강제

### Commit 3 (2603151504) — Phase 2 ROADMAP 재정의

사용자와 Phase 2 방향 논의 후 ROADMAP 재구성.

- 기존 Phase 2 (자율 에이전트 제어) → Phase 3 TBD로 이동
- 새 Phase 2: Gateway + 비서 Agent — tmux 기반 I/O 게이트웨이, CLI 비종속, Telegram 브릿지, 비서 Agent
- 핵심 원칙: 대시보드는 "추가 창구"이지 "유일한 창구"가 아님 (tmux 덕분에 터미널에서도 직접 접근 가능)

### Commit 4 (2603151623) — tmux 세션 관리 + Gateway API

Phase 2 구현 시작 — 토대 레이어.

- `gateway/session_manager.py` — tmux 세션 생성/감지/목록/종료, CLI 프로세스 패턴 매칭 (`/proc` cwd 기반), 외부 CLI 감지
- `data/cli_patterns.json` — 인식할 AI CLI 목록 (claude, codex, opencode, aider 등)
- 서버 API: `GET /api/sessions`, `POST /api/sessions`, `POST /api/sessions/:project/send`, `GET /api/sessions/:project/output`, `DELETE /api/sessions/:project`

### Commit 5 (2603151705) — WebSocket multiplexer + 터미널 뷰 + 상태 인디케이터

실시간 I/O 스트리밍과 대시보드 터미널 UI.

- `gateway/multiplexer.py` — tmux capture-pane 폴링 (0.5초), diff 감지 시 SocketIO emit
- Flask-SocketIO 통합 — subscribe/unsubscribe/send_input 이벤트
- 대시보드 하단 슬라이드업 터미널 패널 — 출력 스트리밍 + 명령 입력 + CLI 시작 버튼
- 컬럼 헤더에 상태 인디케이터 (off=회색, idle=노랑, running=초록 펄스)
- 5초 간격 세션 상태 폴링

## Previous Sessions

2603151211 — 포스트잇 노트 기능, 문서 한글화 키워드 복원, Phase 2 ROADMAP 재정의

2603141524 — Phase 1 대시보드 구축 (파서 + Flask 서버 + 멀티 컬럼 UI), 이후 대규모 UI 개선

2603140119 — 프로젝트 스캐폴드, CLAUDE.md, ROADMAP 수정

## Next Tasks

- [ ] 터미널 뷰 실사용 테스트 및 UX 개선
- [ ] Agent 상태 세분화 (응답 중 vs 응답 완료 vs 유휴)
- [ ] 비서 Agent — MpmWorkspace cwd 채팅
- [ ] Telegram 브릿지
