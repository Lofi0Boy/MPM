# Handoff — 2603161218

## This Session

### Commit 1 (2603161218) — 터미널 UX 버그 수정 모음

- **tmux 마우스 스크롤 수정**: 세션 생성 시 `tmux set mouse on` 적용. 마우스 휠이 명령어 히스토리 탐색 대신 스크롤백 버퍼 탐색으로 정상 작동. 기존 세션도 재접속 시 자동 적용
- **우측 패널 전환 버그 수정**: 모드 전환 시 이전 모드의 인라인 width/height를 제거하여 CSS 기본값이 올바르게 적용되도록 수정. CSS specificity 문제로 `.mode-right.open` 규칙 추가
- **드래그 리사이즈 안정화**: iframe이 마우스 이벤트를 가로채는 문제 해결 — 드래그 중 전체 화면 투명 오버레이 추가
- **터미널 위치/크기 localStorage 저장**: termMode(bottom/right), termW, termH를 localStorage에 저장하여 새로고침 후에도 유지
- **터미널 오버레이 모드**: 터미널 열 때 대시보드 보드가 축소되지 않고, 터미널이 위에 오버레이로 표시
- **ttyd enableSizeOverlay=false 옵션 추가** (ttyd 1.7.4에서 미지원으로 효과 없음, 향후 버전 대비)

## Previous Sessions

2603161121 — xterm.js PTY bridge + CLI 시작 다이얼로그 + 터미널 카드, ttyd 전환 + inotify 파일 감지 + 터미널 패널 리사이즈/우측 토글

2603151818 — 포스트잇 노트, 문서 한글화 키워드 복원, Phase 2 ROADMAP 재정의, tmux 세션 관리 + Gateway API, WebSocket multiplexer + 터미널 뷰

2603141524 — Phase 1 대시보드 구축 + UI 개선

2603140119 — 프로젝트 스캐폴드, ROADMAP 수정

## Next Tasks

- [ ] 키보드 조작 UX — 키보드만으로 모든 기능 이용 가능하게
- [ ] 터미널 활성 프로젝트 시각 효과 — 어떤 프로젝트 터미널이 열려있는지 명확한 표시
- [ ] Agent 상태 세분화 (응답 중 / 응답 완료 / 유휴 / 꺼짐)
- [ ] 응답 완료 인터랙션 — 완료 시 대시보드에서 후속 명령 입력
- [ ] 비서 Agent — MpmWorkspace cwd 채팅
- [ ] Telegram 브릿지
